{{new-question postQuestion1="postQuestion2"}}
{{#each model as |question|}}
  <ul>
    <li>
      <ul>
          <h3>{{question.subject}}</h3>
          {{question.author}}
          {{question.date}}
          <p>{{question.question}}</p>
          {{edit-question editQuestion1="editQuestion2"}}
          <!-- {{#link-to 'show'}}Show{{/link-to}} -->
      </ul>
    </li>
  </ul>

{{/each}}

<!-- {{yield}} -->
