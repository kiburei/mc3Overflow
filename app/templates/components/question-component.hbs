{{#each model as |question|}}
  <ul>
    <li>
      <ul>
          <h3>{{question.subject}}</h3>
          Asked by: {{question.author}}
          on {{question.date}}
      </ul>
    </li>
  </ul>

{{/each}}
{{yield}}
